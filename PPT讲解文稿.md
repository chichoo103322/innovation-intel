# 《用 AI Agent 搭建你的专属工作流》讲解文稿

> 共 14 页，每页对应 PPT 一页。口语化表达，直接照着讲就行。

---

## 第 1 页 · 封面

大家好，今天聊点实在的——怎么让 AI 帮你干活。

我会拿我们自己团队在用的一个东西举例子。我们每天要出一份叫"创新常州·对标快讯"的日报，收集全国的政策动态和科技情报，整理成 PDF 发给领导看。以前一个人做这件事，三四个小时。现在呢？我打开电脑，打字"出日报"，10 分钟后 PDF 出现在桌面上，排版、校验、分发全自动搞定。

怎么做到的？接下来我一步步讲。

---

## 第 2 页 · 先看效果

先让大家看这个东西实际是怎么跑的。

第一步，在自己电脑上装 Claude Code。一行命令的事情。然后接入 DeepSeek 的 API。一个月成本 25 块钱，不需要什么 GPU 服务器，普通办公电脑就行。

第二步，把这个项目文件夹往 CC 窗口里一拖。Agent 会自动识别这是一个项目，把里面的规则文件全部加载好——什么能做、什么不能做、按什么流程来，全部就绪。什么都不用配。

（这页右边有个截图，就是拖进去以后的实际画面）

第三步，在窗口里打三个字："出日报"。回车。然后就不用管了。

Agent 接下来会用 CC 自带的 WebSearch 工具直接调搜索引擎，分四个维度搜十几次。搜到的每条结果——标题、摘要、URL——它会再用 WebFetch 逐条点进去验证：页面能不能访问？内容对不对得上？数据准不准确？不行的直接丢掉。验证通过的写成 JSON，然后自动跑校验脚本，有错自己改，改完再验，直到零错误。最后用 Chrome 渲染成 PDF，发到桌面上。

十分钟出头，完事。

而且你还可以让它定时跑。直接在 CC 窗口里对它说一句："每天下午四点自动执行出日报，推到桌面。"Agent 自己会设好定时触发，到点自动启动全套流程——你不用在电脑前面，四点一到，它自己搜、自己验、自己写、自己发。等你回到座位上，PDF 已经在桌面上了。

---

## 第 3 页 · 零基础部署：从安装到跑起来

前面看了效果，有人可能觉得这得懂技术才能搞。完全不需要。这一页就是给完全零基础的人准备的，照着做就行。

**第一步，安装 Claude Code。**

Mac 上打开"终端"——在启动台搜 Terminal 就能找到。Windows 上搜 PowerShell。打开之后是一个黑窗口，别怕，复制粘贴这一行，回车：

`npm install -g @anthropic-ai/claude-code`

如果提示 npm 找不到，先去 nodejs.org 下载安装 Node.js（点绿色的 LTS 按钮下载，一路下一步就行），装完再回来执行上面那行命令。

**第二步，接入 API。**

推荐用 DeepSeek，最便宜，一个月 25 块钱够用。先去 DeepSeek 官网注册一个账号，在控制台里找到 API Key，复制下来。然后在终端里执行：

`claude --provider deepseek --model deepseek-v4-pro --api-key 你的KEY`

这条命令告诉 CC：以后干活用 DeepSeek 这个模型来思考。你用 OpenAI 的 API 或者 Anthropic 自己的 API 也一样，把 provider 和 model 换一下就行。

**第三步，装 Skill 插件。**

这个是可选的，按需装。在 CC 窗口里直接说话就行："帮我安装 PDF Viewer 插件"——以后就能查看和批注 PDF。"帮我安装 Frontend Slides 插件"——能自动生成 PPT。不用懂技术，说话就行。Agent 会在需要的时候自动调用这些插件。

装完之后，把项目文件夹往 CC 窗口里一拖，Agent 就自己开工了。从零到跑起来，十分钟的事。不需要学代码，不需要搭环境，不需要买服务器。

---

## 第 4 页 · Agent 不是你想象的那种 AI

很多人觉得 AI 就是你问一句它答一句。不是的。

你问它答那个叫 Chatbot。Agent 能做的事情多得多——它能读你电脑上的文件、能执行脚本、能打开网页验证信息。它是一个执行器，不是一个答题器。

比如有人说："可是我每次用 AI 都要重新解释一遍我要干嘛，好烦。"那是因为你没写 CLAUDE.md。你把项目规则写一次放进去，以后 Agent 每次打开都自动读。换台电脑、换个人，它行为一模一样。

有人说："AI 又不能碰我电脑上的文件。"Agent 可以。创建文件、修改文件、跑 Python、调 Chrome 渲染 PDF——全都能干。

所以你要换个思路。你不是 AI 的使用者，你是工作流的定义者。你定规则，它来跑。

你要做的就三件事：写个提示词告诉它怎么干活，写个 CLAUDE.md 定项目规矩，碰到专业需求就给它装个 Skill 插件。没了。

---

## 第 5 页 · 怎么写提示词

写提示词这件事，跟写工作交接文档差不多。你要交代清楚三样东西。

第一，边界。什么能干、什么不能干，要说死。比如我们项目里写了："只收录 29 座万亿城市，小城市不管"、"日报只采当天发布的内容"。边界比能力重要——你知道它能做什么不重要，知道它不能做什么才重要。

第二，标准。你说"内容质量要好"，它理解不了什么叫"好"。但你说"每条要经过 6 层验证""每板块至少 1 条来自 .gov.cn""每条出 3 版不同角度的建议"，它就能执行，你也能检查。

第三，步骤。第一步干嘛、第二步干嘛，一步一步来，每步要有产出。这样中间哪步出问题了，一眼就知道。

四个原则记住就行：边界第一，标准要能验证，步骤要能打断检查，出了错就加规则别删数据。

---

## 第 6 页 · 一个提示词的进化过程

给你看我们项目的真实经历。提示词不是一次性写完美的，是慢慢改出来的。

第一版就 29 行，简单说"你是情报分析师，去这几个网站找信息，分四类整理"。能用，但质量不敢保证。

第二版开始加规则了。有次我们发现报告里收录了洛阳——洛阳 GDP 没过万亿，不在我们关注范围里。怎么办？不是删掉洛阳这条，是把 29 座万亿城市全写进白名单里。以后搜索阶段就直接过滤。

第三版，发现有人把"科技委"写成了"科委"。日期也经常填错——填的是网页发布日期，不是事件发生日期。于是加了机构名硬约束和日期校验规则。

第四版改了搜索方式。原来搜"管理办法"这种词，搜出来的东西命中率只有 30%。后来换成"概念验证中心""揭榜挂帅""先投后股"这些实际业务词汇，命中率直接到 80%。

第五版最关键。我们发现有些 URL 是 AI 编的，根本打不开。于是加了两道验证——每条 URL 写入前去访问一次确认能打开，全部写完之后再全量复查一遍。同时固化了"出日报"这个触发词。

28 次迭代，没有一次是改数据本身的。全部是改提示词、改规则。提示词是长出来的，不是写出来的。

---

## 第 7 页 · CLAUDE.md，给项目立规矩

提示词管的是"这一次任务怎么做"，但整个项目的底线谁来管？

CLAUDE.md。一个放在项目根目录的纯文本文件。Agent 每次启动都会自动读它。

我们项目里定了四个优先级。P0 是真实性——不能编 URL，不能编数据，做不到就少一条，不凑数。P1 是零打扰——干活就干活，别老问我"要不要继续""这样行不行"，自己决定自己执行。P2 是万亿城市白名单。P3 是格式规范。

关键规矩是：高优先级绝对不能给低优先级让步。你不能为了凑够 8 条内容（P3）去编造一个 URL（违反 P0）。

CLAUDE.md 里还定义了触发词。"出日报""出周报""出月报"——Agent 看到这三个词就知道该启动哪个流程。

而且这个文件跟着代码走。别人把项目拷过去，打开 CC，行为和你这完全一样。不需要口头交接，不需要培训。

一句话：提示词是操作手册，CLAUDE.md 是宪法。

---

## 第 8 页 · 把任务拆成 Agent 能执行的步骤

有了提示词和 CLAUDE.md，接下来就是拆任务。

"做一份日报"这句话太模糊了，Agent 没法执行。你要拆成它能一步步做的事情。

我们拆成了 6 步：

第一步，读规则。Agent 加载提示词和 CLAUDE.md，知道今天搜什么、什么标准能过。

第二步，Agent 调用 CC 自带的 WebSearch 工具，按四个维度各自搜 3 到 5 次，总共 15 到 20 次。WebSearch 是 CC 内置的，直接调搜索引擎，返回标题、摘要和 URL，不是去爬特定网站。覆盖面是全网，速度很快。

第二步半——这是我们后来硬加进去的。WebSearch 返回的结果只是标题和摘要，内容是不是真的、能不能访问，不知道。所以每条结果 Agent 必须再用 WebFetch 工具打开 URL 逐条验证：页面能不能访问？标题对不对得上？里面的数据跟我写的一样吗？不通过直接丢。这步要是没有，后面全是白干。

第三步，把通过验证的写成 JSON 数据。

第四步，跑校验脚本。格式有问题、字数不对、缺少必填字段——Agent 自己发现自己改，改完再跑校验，直到全部通过。

最后两步，渲染成 PDF，发到桌面。

三个原则：每步有输入有输出，卡在哪里一眼就知道。验证步骤一个都不能省——省掉就是埋雷。步骤顺序本身就是质量防线，把检查点插在容易出错的地方。

---

## 第 9 页 · Skill，给 Agent 装插件

Agent 本身是通用的。但你要它处理 PDF、生成幻灯片、查数据库，它不会——除非你给它装 Skill。

Skill 就是插件。我们项目用了三个：

PDF Viewer——Agent 能打开 PDF 预览、批注、提取文字。我们用来检查生成的日报排版。

Frontend Slides——大家现在看到的这份 PPT，就是 Agent 用这个 Skill 自动生成的。一个 HTML 文件，浏览器打开直接翻页。

BigQuery——Agent 直接写 SQL 查数据库，分析趋势。

本质很简单：把一个人的专业技能封装成 Agent 能调用的工具。任何重复性的专业工作，封装成 Skill，Agent 就有了这个领域的能力。

---

## 第 10 页 · Codex 还是 Claude Code？怎么选

市面上 Agent 产品不少，聊两个最有代表性的：OpenAI Codex 和 Claude Code。它俩定位完全不一样。

Codex 像大脑。代码理解和生成能力顶级，复杂推理很强。在云端沙箱跑，环境隔离。API 和 SDK 丰富，适合嵌到现有系统里。最适合干什么？写代码、架构设计、重构、查 Bug——纯编程的事情。

Claude Code 像双手。它是终端原生的，直接操作你电脑上的文件系统。能跑 Shell、执行 Python、做 Git 全套。它有个独特优势是 CLAUDE.md 的项目级记忆，还有丰富的 Skill 插件。最适合干什么？端到端的自动化流程——要操作文件、跑脚本、编排多步骤的那种活。

我们项目为什么选 CC？五个原因：要大量读写文件（JSON、HTML、PDF）；要跑 Shell 和 Python 脚本；要编排多步骤流程（搜→验→写→校→出）；CLAUDE.md 让项目有持久记忆和触发词；拖文件夹进去说三个字就完事，够简单。

你怎么选？写代码为主 → Codex。要操作文件、跑脚本、编排流程 → Claude Code。两者结合也行——Codex 当脑、CC 当手。没有万能工具，看你的活是什么。

---

## 第 11 页 · 迭代方式：改规则，别改数据

传统开发修 Bug 要改代码、编译、测试、部署。Agent 项目简单得多——改规则就行。

举五个我们踩过的坑：

1. 洛阳不是万亿城市，但我们看到的时候它已经在报告里了。做法：提示词里加 29 城白名单，以后自动过滤。不是只删洛阳这一条。

2. 日报和周报有重复内容。做法：做了三层去重——标题精确匹配、关键词相似度、事件指纹，三重保险。

3. 搜"管理办法"命中率才 30%。做法：改用"概念验证中心"这种实义词，命中率 80%。

4. 发现 URL 可能是 AI 自己编的，点进去 404。做法：写入前验证一次，写完再全量复核一次。

5. Agent 老问"要不要继续？""这个行吗？"烦。做法：CLAUDE.md 里写死，不准问，自己判断。

核心逻辑就两个：第一，不是修一条数据，是修一个规则。加一条规则，所有同类问题全部消失。第二，提示词和校验脚本配合——提示词告诉它怎么做，脚本验证它做没做对。双保险。

项目 28 次代码提交，没有一次是改数据的，全是改规则。

---

## 第 12 页 · 三个阶段的变化

同一份日报，三个阶段真实对比。

第一阶段，纯手工。每天手动翻五十多个网站，复制粘贴到 Word，人眼判断，手动排版。错误率大概 15%，没有去重没有白名单。三四个小时一份。

第二阶段，半自动。接入其他模型的 API 来做搜索，但问题很大——URL 经常是编造的，点进去 404，数据对不上。所以搜完还得人工一条条核实，JSON 模板加脚本生成 PDF，gov.cn 优先过滤。有基础提示词了，但人还是离不开。一两个小时。

第三阶段，全自动。关键在于 Agent 自己调用工具——CC 自带的 WebSearch 搜完全网，再逐个 WebFetch 打开 URL 验证真假，整个链条 Agent 自己判断、自己执行。6 层验证、三版洞察自动出、三层去重、格式零错误。说一句话触发，人不用管。十到十五分钟。

效率提了大概 20 倍。AI 成本一个月 25 块。

---

## 第 13 页 · 你也能做

不需要会写代码。你会描述自己的工作流程就够了。

三步：

找一件你每天或每周重复做的事。日报、周报、数据汇总，什么都行。条件就一个——每次做的步骤差不多。

写第一版提示词。告诉它你是什么角色、要干什么、什么不能干、怎么算合格。十几行字就够。

用一次改一次。第一次肯定不会完美。发现一个问题就加一句规则。用十次就稳定了，用二十次基本不乱出错了。

启动清单：装 CC，接一个 API 账号，建个项目文件夹，写提示词，写 CLAUDE.md，装需要的 Skill，把文件夹拖进 CC，开口说你要什么。

最后一个心态问题。你不是在用一个工具，你是在训练一个助手。给它规则，它会执行。给它反馈，它会变好。用久了它会越来越懂你。跟你带一个新同事是一回事，差别是这个同事不用睡觉，跑得还快。

---

## 第 14 页 · 结束

三句话总结：

提示词是你的经验变成 Agent 的行为逻辑。

CLAUDE.md 是你给项目划的底线，写一次管一辈子。

Skill 是专业能力的封装，让 Agent 的能力无限扩展。

找一个适合你的 Agent，把你的工作流定义清楚，让它去跑。

谢谢。
