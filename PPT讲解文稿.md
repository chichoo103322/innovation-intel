# 《用 Agent 搭建你的专属情报官》讲解文稿

> 面向零基础同事的内部分享。口语化表达，直接照着讲就行。

---

## 第 1 页 · 封面

大家好，今天不讲大道理，咱们聊点能立刻解放双手的干货。

大家平时都在做情报收集，我知道这活儿特别苦：每天要刷几十个网站，看几百条信息，还要筛选、排版、写分析，一份《对标快讯》做下来，半天就没了。

今天我就演示一下，怎么用 AI Agent 把这事儿全包了。

现在，我只需要对着电脑说三个字："出日报"。然后去喝杯咖啡，10 分钟后，一份排版精美、校验完毕、带有"创新洞察"的 PDF 就躺在桌面上了。

怎么做到的？接下来我一步步拆解。

---

## 第 2 页 · 先看效果

先让大家感受一下这个项目实际怎么跑。不讲原理，直接看。

打开 Claude Code 的窗口，把项目文件夹往里一拖。Agent 自动识别，里面的规则文件全部加载好——什么能做、什么不能做、什么流程，全部就绪。什么都不用配。

（左边第一张截图，就是拖进去以后的 CC 窗口画面。）

然后在窗口里打三个字："出日报"。回车。Agent 就开始自己干活了。

（中间这张截图，就是输入"出日报"之后 Agent 开始工作的画面。）

接下来什么都不用管。Agent 自己用 WebSearch 搜全网，WebFetch 逐条打开验证，通过的写 JSON，跑校验，有错自己改，最后渲染成 PDF 发到桌面。全程 10 到 15 分钟。

（右边是等待完成的画面。）

而且你还可以让它定时跑。对它说一句："每天下午四点自动执行出日报，推到桌面。"到点就自己开工——你不在电脑前面也没关系，回来 PDF 已经在桌面上了。

---

## 第 3 页 · 零基础部署：3 步上手

有人问："这听起来很厉害，但我不会写代码啊。"

放心，这套工具就是给咱们办公人员设计的，不需要买服务器，不需要懂编程。只要三步：

**第一步，装工具。** 在你的办公电脑上装一个 Claude Code（简称 CC）。就像装微信一样简单，一行命令搞定。每个人电脑环境不一样，遇到问题网上搜"Claude Code 安装教程"，看个视频跟着做，不超过十分钟就能装好。

**第二步，接入口。** 推荐用 DeepSeek 的 API，性价比极高，一个月只要 25 块钱，比一杯咖啡还便宜。注册 DeepSeek 账号，拿到 API Key，然后网上搜索下载一个叫 **CC Switch** 的小工具，一键就能接入 DeepSeek API，不用手动敲命令。连上网，工具就有"脑子"了。

**第三步，拖入项目。** 我把咱们"创新情报"这个项目文件夹往窗口里一拖，所有的规则——比如"只关注 29 座万亿城市"、"必须包含 .gov.cn 信源"——瞬间加载完毕。

到此为止，环境搭建好了。接下来，你就是老板，指挥它干活就行。

---

## 第 4 页 · Agent 不是聊天机器人

我们要转变一个观念：Agent 不是 Siri，你问一句它答一句。Agent 是你的数字员工。

它有三大能力，是普通 AI 没有的：

能操作电脑——它能自己打开网页、读写文件、运行校验程序。

有长期记忆——它通过一个叫 CLAUDE.md 的文件记住咱们的规矩。比如"严禁编造链接"、"上海国创中心的动态必须优先"。

能调用插件——比如让它看 PDF、生成 PPT。

所以，你不再是那个苦哈哈的"打字员"，而是制定规则的"管理者"。你定好标准，它负责执行。你要做的就三件事：写提示词、写 CLAUDE.md、装 Skill 插件。没了。

---

## 第 5 页 · 怎么写提示词

提示词就是 Agent 的"工作说明书"。像带新人一样带 Agent。

我们给 Agent 的指令非常具体，绝不说"你去搜集一下政策"这种模糊的话，而是像这样分解：

第一，边界。什么能干、什么不能干，要说死。比如"只收录 29 座万亿城市"、"日报只采当天发布的内容"。边界比能力重要。

第二，标准。你说"内容质量要好"，它理解不了。但你说"每条要经过 6 层验证""每板块至少 1 条来自 .gov.cn""每条出 3 版不同角度的建议"，它就能执行，你也能检查。

第三，步骤。第一步干嘛、第二步干嘛，一步一步来，每步要有产出。这样中间哪步出问题了，一眼就知道。

四个原则：边界第一，标准要能验证，步骤要能打断检查，出了错就加规则别删数据。

---

## 第 6 页 · 提示词是这样打磨出来的

前面讲了原则。那具体怎么操作？不是一个人闷头写，是人 + AI + Agent 三方协作。

**第一步，和 AI 一起写初版。** 把你自己的业务需求讲给 AI 听——你是干什么的、每天要产出什么、什么标准算合格。AI 帮你把这些整理成结构化的提示词。你自己最懂业务，AI 最懂怎么写提示词，结合起来。

（左边这张截图，就是我在和 ChatGPT 讨论提示词怎么改。把当前版本的提示词发过去，告诉它哪里不满意，它给具体的修改建议。）

**第二步，根据 Agent 的产出，不停地找不足，和 AI 讨论优化。** 初版提示词写好了，丢给 Agent 执行。跑出来的结果肯定不完美——格式不对、漏了维度、标准太模糊。这时候把产出拿去和其他 AI 讨论："你看这个结果，问题在哪里？提示词应该怎么改？" AI 会帮你分析，给出具体的优化方向。改完提示词再让 Agent 跑，跑完再找不足、再讨论、再改。反复几轮。

（中间这张截图，就是 Agent 跑出来的结果。我把问题拿去和 AI 讨论，让它帮我分析哪里不对、怎么改提示词。）

**第三步，和 Agent 一起改。** 你把问题告诉 Agent："这里不对，应该这样""那个标准太模糊了，改成具体数字"。Agent 自己修改提示词，然后再跑一遍。跑完再看，还有问题继续改。一般来回三四轮就基本稳定了。

（右边这张截图，就是我在和 Agent 逐条讨论修改方案。Agent 根据反馈自己调整提示词。）

这个循环走通了，你就有了一个真正好用的提示词。核心逻辑：AI 帮你起草，Agent 帮你验证，你只负责判断"对不对"。

---

## 第 7 页 · 一个提示词的进化过程

刚才讲的是方法，现在给你看我们项目的真实经历。提示词不是一次性写完美的，是慢慢改出来的。

第一版就 29 行，简单说"你是情报分析师，去这几个网站找信息，分四类整理"。能用，但质量不敢保证。

第二版开始加规则了。有次我们发现报告里收录了洛阳——洛阳 GDP 没过万亿，不在我们关注范围里。怎么办？不是删掉洛阳这条，是把 29 座万亿城市全写进白名单里。以后搜索阶段就直接过滤。

第三版，发现有人把"科技委"写成了"科委"。日期也经常填错——填的是网页发布日期，不是事件发生日期。于是加了机构名硬约束和日期校验规则。

第四版改了搜索方式。原来搜"管理办法"这种词，搜出来的东西命中率只有 30%。后来换成"概念验证中心""揭榜挂帅""先投后股"这些实际业务词汇，命中率直接到 80%。

第五版最关键。我们发现有些 URL 是 AI 编的，根本打不开。于是加了两道验证——每条 URL 写入前去访问一次确认能打开，全部写完之后再全量复查一遍。同时固化了"出日报"这个触发词。

28 次迭代，没有一次是改数据本身的。全部是改提示词、改规则。提示词是长出来的，不是写出来的。

---

## 第 8 页 · CLAUDE.md：定规矩

提示词管的是"这一次任务怎么做"，但整个项目的底线谁来管？

CLAUDE.md。一个放在项目根目录的纯文本文件。Agent 每次启动都会自动读它。对于我们情报工作来说，这就是"编辑部准则"。

我们项目里定了四个优先级。P0 是真实性——绝对禁止编造 URL 和数据，搜不到就少一条，不凑数。P1 是零打扰——干活就干活，别老问我"要不要继续"，自己决定自己执行。P2 是万亿城市白名单，像之前误收"洛阳"的情况，在规则里直接被过滤。P3 是格式规范，包括信源分级，优先采用 .gov.cn 官方网站信息。

关键规矩是：高优先级绝对不能给低优先级让步。你不能为了凑够 8 条内容（P3）去编造一个 URL（违反 P0）。

CLAUDE.md 里还定义了触发词。"出日报""出周报""出月报"——Agent 看到这三个词就知道该启动哪个流程。

好处是什么？只要这个文件夹拷贝给科室的任何一位同事，他们打开 CC，都能生成一模一样的报告。不需要培训，不需要交接。

一句话：提示词是操作手册，CLAUDE.md 是宪法。

---

## 第 9 页 · 把任务拆成 Agent 能执行的步骤

有了提示词和 CLAUDE.md，接下来就是拆任务。

"做一份日报"这句话太模糊了，Agent 没法执行。你要拆成它能一步步做的事情。

我们拆成了 6 步：

第一步，读规则。Agent 加载提示词和 CLAUDE.md，知道今天搜什么、什么标准能过。

第二步，Agent 调用 CC 自带的 WebSearch 工具，分四个维度——科技委动态、上海国创中心、科创政策、改革举措——各自搜 3 到 5 次，总共 15 到 20 次。不是去爬特定网站，是直接调搜索引擎，覆盖面是全网。

第二步半——这是我们后来硬加进去的。WebSearch 返回的结果只是标题和摘要，内容是不是真的、能不能访问，不知道。所以每条结果 Agent 必须再用 WebFetch 工具打开 URL 逐条验证：页面能不能访问？标题对不对得上？里面的数据跟我写的一样吗？不通过直接丢。这步要是没有，后面全是白干。

第三步，把通过验证的写成 JSON 数据。

第四步，跑校验脚本。格式有问题、字数不对、缺少必填字段——Agent 自己发现自己改，改完再跑校验，直到全部通过。

最后两步，渲染成 PDF，发到桌面。

三个原则：每步有输入有输出，卡在哪里一眼就知道。验证步骤一个都不能省——省掉就是埋雷。步骤顺序本身就是质量防线，把检查点插在容易出错的地方。

---

## 第 10 页 · Skill：给 Agent 装插件

Agent 本身很聪明，但有些专业活儿它需要工具。这就用到 Skill（插件）。

在我们项目里用了三个：

PDF Viewer——让 Agent 能看懂 PDF 里的文字，方便它提取复杂的政策文件内容。我们用来检查生成的日报排版。

Frontend Slides——大家现在看到的这份 PPT，就是 Agent 用这个 Skill 自动生成的。以后不仅要出日报，如果要出 PPT 汇报，Agent 也能直接把情报整理成演示文稿。

BigQuery——如果涉及到统计数据，Agent 可以自动写 SQL 查数据库，分析趋势。

本质很简单：把一个人的专业技能封装成 Agent 能调用的工具。任何重复性的专业工作，封装成 Skill，Agent 就有了这个领域的能力。

---

## 第 11 页 · Codex 还是 Claude Code？怎么选

市面上 Agent 产品不少，聊两个最有代表性的：OpenAI Codex 和 Claude Code。它俩定位完全不一样。

Codex 像大脑。代码理解和生成能力顶级，复杂推理很强。在云端沙箱跑，环境隔离。API 和 SDK 丰富，适合嵌到现有系统里。最适合写代码、架构设计、重构、查 Bug——纯编程的事情。

Claude Code 像双手。它是终端原生的，直接操作你电脑上的文件系统。能跑 Shell、执行 Python、做 Git 全套。它有个独特优势是 CLAUDE.md 的项目级记忆，还有丰富的 Skill 插件。最适合端到端的自动化流程——要操作文件、跑脚本、编排多步骤的那种活。

我们项目为什么选 CC？五个原因：要大量读写文件（JSON、HTML、PDF）；要跑 Shell 和 Python 脚本；要编排多步骤流程（搜→验→写→校→出）；CLAUDE.md 让项目有持久记忆和触发词；拖文件夹进去说三个字就完事，够简单。

你怎么选？写代码为主 → Codex。要操作文件、跑脚本、编排流程 → Claude Code。两者结合也行——Codex 当脑、CC 当手。没有万能工具，看你的活是什么。

---

## 第 12 页 · 迭代方式：改规则，别改数据

传统开发修 Bug 要改代码、编译、测试、部署。Agent 项目简单得多——改规则就行。

举五个我们踩过的坑：

1. 洛阳不是万亿城市，但我们看到的时候它已经在报告里了。做法：提示词里加 29 城白名单，以后自动过滤。不是只删洛阳这一条。

2. 日报和周报有重复内容。做法：做了三层去重——标题精确匹配、关键词相似度、事件指纹，三重保险。

3. 搜"管理办法"命中率才 30%。做法：改用"概念验证中心""揭榜挂帅""先投后股"这种实义词，命中率 80%。

4. 发现 URL 可能是 AI 自己编的，点进去 404。做法：写入前验证一次，写完再全量复核一次。

5. Agent 老问"要不要继续？""这个行吗？"烦。做法：CLAUDE.md 里写死，不准问，自己判断。

记住这个公式：发现一个错误 + 增加一条规则 = 解决一类问题。项目 28 次代码提交，没有一次是改数据的，全是改规则。

---

## 第 13 页 · 三个阶段的变化

同一份日报，三个阶段真实对比。

第一阶段，纯手工。每天手动翻五十多个网站，复制粘贴到 Word，人眼判断，手动排版。错误率大概 15%，没有去重没有白名单。三四个小时一份。

第二阶段，半自动。接入其他模型的 API 来做搜索，但问题很大——URL 经常是编造的，点进去 404，数据对不上。所以搜完还得人工一条条核实，JSON 模板加脚本生成 PDF，gov.cn 优先过滤。有基础提示词了，但人还是离不开。一两个小时。

第三阶段，全自动。关键在于 Agent 自己调用工具——CC 自带的 WebSearch 搜完全网，再逐个 WebFetch 打开 URL 验证真假，整个链条 Agent 自己判断、自己执行。6 层验证、三版洞察自动出、三层去重、格式零错误。说一句话触发，人不用管。十到十五分钟。

效率提了大概 20 倍。AI 成本一个月 25 块。

---

## 第 14 页 · 你也能做

不需要会写代码。你会描述自己的工作流程就够了。

三步：

找一件你每周重复做的事。日报、周报、数据汇总，什么都行。条件就一个——每次做的步骤差不多。

写第一版提示词。像写工作交接文档一样，写下操作步骤和标准。十几行字就够。

用一次改一次。第一次肯定不会完美。发现一个问题就加一句规则。用十次就稳定了，用二十次基本不乱出错了。

启动清单：装 CC → 接 API → 建文件夹 → 写规则 → 拖进去 → 下指令。

最后一个心态问题。你不是在用一个工具，你是在训练一个助手。给它规则，它会执行。给它反馈，它会进步。跟你带一个新同事是一回事，差别是这个同事不用睡觉，跑得还快。

---

## 第 15 页 · 总结

三句话总结：

提示词是把你的经验固化成了 Agent 的行为逻辑。

CLAUDE.md 是你给项目划下的底线，写一次，管到底。

Skill 是专业能力的封装，让 Agent 无所不能。

对于我们的创新情报工作而言，Agent 的价值不在于替代人，而在于把人从繁琐的"查找、复制、粘贴、排版"中解放出来，让我们能把更多精力花在"创新洞察"和"决策建议"上。

未来，我们不仅有"日报 Agent"，还可以有"政策分析 Agent"、"招商线索 Agent"。让我们一起把重复的劳动交给 AI，把智慧的火花留给自己。

谢谢大家！
