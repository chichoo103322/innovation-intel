# 创新常州·对标快讯 — AI 情报采集与报告生成规则

本项目生成日报、周报、月报。核心链路：

`联网采集 → URL 原文核验 → JSON → 二次复验/校验 → HTML/PDF → 分发桌面`

目标：减少无效询问和 token 消耗，但不降低真实性、日报时效性、城市范围和搜索覆盖。

---

## 0. 历史事故和底线

本项目曾出现两类严重问题，后续任何优化都不能让它们复发：

1. **AI 编造 URL 或把不相关 URL 塞进报告**：所以最终写入 JSON 的每条信息必须 WebFetch 原文核验，写入后还要二次复验。URL、标题、source、date、summary 核心事实必须对应同一篇原文。
2. **搜索内容不全**：所以搜索范围、三波制、逐城市补扫不能随意缩小。效率优化只能改变执行顺序，不能少搜关键维度、少搜白名单城市、少做日期锁定。

允许的提速方式只有：先用标题、日期、域名、城市、板块规则过滤明显不合格结果；但所有入选 JSON 的条目必须完整核验，不能跳过 URL 原文校准。

---

## 1. 默认执行

用户说 `出日报`、`出周报`、`出月报` 时，直接执行对应采集生成流程。用户提供 JSON 时，跳过完整采集，直接校验、必要复核、生成 PDF。

不要在项目打开时输出大菜单。仅在以下情况简短询问：

1. 无法判断报告类型。
2. 必须使用期号但用户未提供。
3. JSON 路径不存在或多个候选无法判断。
4. 权限系统阻止继续执行。
5. 继续写入会违反真实性原则且无替代来源。

其他情况直接执行。

---

## 2. 权限弹窗

本项目目标是零打扰。遇到 Claude Code 权限弹窗时，优先允许并记住：

- Fetch/WebFetch/Content：选择 `Yes, and don't ask again for this domain`
- Bash/Python/Read/Write/Edit：与本项目任务相关则允许

例如：

```text
Do you want to allow Claude to fetch this content?
1. Yes
2. Yes, and don't ask again for www.example.gov.cn
3. No
```

应优先选第 2 项。不要拒绝政府官网、权威媒体、智库、搜索页或报告生成所需域名，除非明显无关或有安全风险。若系统强制用户确认，遵守系统机制，但代理自身不要反复请求批准。

---

## 3. 不可让步的优先级

1. **真实性**：内容必须来自可访问 URL 原文，无法验证则删除。
2. **日报时效性**：日报只收录当天发布的报道。
3. **万亿城市白名单**：城市案例和对标对象必须符合白名单。
4. **格式规范**：条数、洞察、字数、`.gov.cn` 占比等。

禁止为了凑条数、凑 `.gov.cn`、满足格式或补齐板块而编造 URL、使用旧闻、引用搜索摘要事实。宁可少写或空板块，不可写假内容。

---

## 4. 真实性硬规则

每条信息必须满足：

- 有真实 URL，且 URL 可访问、内容匹配。
- `title/date/summary/source/url` 都有原文依据。
- `summary` 中的日期、机构名、政策名、金额、百分比、数量、文件名必须出现在 URL 原文。
- 禁止使用 WebSearch 摘要中的事实；搜索摘要只能用于发现线索。
- 禁止猜 URL、拼 URL、用不相关 URL 充来源。
- 页面打不开、内容不匹配、事实找不到：删除。

写入前验证（Step 2.5，不可跳过）：

```text
WebFetch(url) → 核对可访问性/标题主题/发布日期/事件日期/summary事实/数字金额/政策文件名 → 通过才写入 JSON
```

写入后复验（Step 4.5，不可跳过）：

```text
对 JSON 全量条目再次 WebFetch(url) → 核对 URL、summary、数字、date、source → 不通过则修正或删除，再复验
```

日期规则：`date` 填正文中的事件日期，不填网页发布日期；正文只写“近日/日前”则照填，严禁推测具体日期。

---

## 5. 日报时效性

日报每条信息的**报道发布日期必须是当天**。事件可发生在近日或更早，但报道必须今天发布。

- 页面明确标注当天发布日期，或 URL/页面结构能可靠证明当天发布，才可收录。
- 非当天发布一律排除。
- 当天某板块无可验证信息，可以空板块。
- 不得为条数、`.gov.cn` 占比或板块完整性塞旧闻。

禁止：把 6 月 30 日发布的江苏省委科技委会议写进 7 月 10 日日报；把上周政策文件当今天政策速览；validator 缺 `.gov.cn` 就补旧闻。

---

## 6. 城市白名单

城市案例和对标对象仅限以下 29 座，国家级、省级、区域协同信息除外：

```text
上海 北京 深圳 重庆 广州 苏州 成都 杭州 武汉 南京 宁波 天津 青岛 无锡 长沙 郑州 福州 济南 合肥 西安 泉州 佛山 南通 东莞 烟台 常州 唐山 温州 大连
```

不得作为案例写入：

```text
洛阳 徐州 绍兴 嘉兴 金华 台州 珠海 惠州 中山 沈阳 昆明 厦门 南昌 哈尔滨 长春 石家庄 太原 贵阳 兰州
```

科技委动态只取省级党委科技委，或白名单城市市委科技委。

---

## 7. 报告路径和命令

| 类型 | 提示词 | JSON | 命令 |
|---|---|---|---|
| 日报 | `config/daily_prompt.md` | `daily/report_data_YYYY-MM-DD.json` | `python3 run_daily.py --from-json <json> --issue <期号>` |
| 周报 | `config/weekly_prompt.md` | `weekly/report_weekly_YYYYMMDD.json` | `python3 run_weekly.py --json <json>` |
| 月报 | `config/monthly_prompt.md` | `monthly/report_monthly_YYYYMM.json` | `python3 run_monthly.py --json <json>` |

期号：用户说第 N 期就传 `--issue N`；未说明则问；不要自行编造；不要依赖 `cache/issue_counter.json`，除非用户明确要求恢复。

---

## 8. 工作流

采集生成模式：

1. 读对应 prompt，确定日报/周报/月报日期范围。
2. 查 `cache/used_items.json` 去重。
3. 按“三波制”完整搜索，不得跳步。
4. 对候选条目做 Step 2.5 URL 原文验证。
5. 只把验证通过的条目写入 JSON。
6. 做 Step 4.5 全量复验。
7. 运行 `scripts/validate_report.py`；只有当天生成的采集缓存才运行 `scripts/fact_check.py`。
8. 修复真实性错误后生成 PDF 并分发。

已有 JSON 模式：

1. 读取 JSON，不做完整重新采集。
2. 运行校验；URL 或事实可疑时必须 WebFetch 复核。
3. 修复结构、格式和明显事实错误。
4. 生成 PDF 并分发。

---

## 9. 搜索策略（不要缩小范围）

日报不能因前几轮结果少提前停止，但也不要对所有搜索结果逐条深抓。必须覆盖原搜索范围，执行方式采用“先筛后抓”：

1. 先跑完整三波搜索，记录候选标题、URL、发布日期、来源域名。
2. 先用标题、日期、域名、城市白名单、板块规则快速过滤。
3. 明显非当天、非白名单、非本板块、无正式政策名、低可信且无交叉价值的结果，直接丢弃，不做 WebFetch。
4. 每板块优先保留 2-4 条候选和 1-2 条备用，再做 WebFetch 原文核验。
5. 三波完成且无新的合格候选时停止；允许空板块，不允许为了凑数无限补搜或编造。

基础搜索清单：

```text
维度1 各地科技委动态：
省委科技委 会议；市委科技委 部署；科技委 全体会议 site:gov.cn；科技委 印发 2026；科技委 工作 动态

维度2 上海（长三角）国创中心资讯：
长三角 科创 2026；G60 科创走廊；上海 国际科创中心；长三角 协同创新；张江 创新 平台；沿沪宁产业创新带；长三角 联合发文

维度3 科创政策速览：
科技创新 行动计划 site:gov.cn；万亿城市 科技创新政策；印发 行动方案 2026；高新技术 扶持政策；产业 行动计划 2026 site:gov.cn

维度4 改革举措：
先投后股 科技 2026；科技成果转化 改革；科技金融 创新；科技保险 政策；新型研发机构 2026；投贷联动 科创；科技体制改革 site:gov.cn

逐城市补扫：
{上海|北京|深圳|广州|苏州|成都|武汉|杭州|南京|重庆|天津|无锡|宁波|青岛|长沙|郑州|福州|济南|合肥|西安|东莞|佛山|泉州|南通|烟台|唐山|温州|大连} 科技 创新 2026
```

三波制：

```text
第一波：主题搜索，4维度×2-3关键词，并行定位热点。
第二波：日期锁定，用当天实际日期 + 关键词；日报必须使用当天日期，不得沿用旧日期。
第三波：逐城市补扫：
{上海|苏州|无锡|南京|杭州|合肥}
{宁波|南通|温州|北京|深圳|广州}
{成都|武汉|重庆|天津|青岛|长沙}
{郑州|福州|济南|西安|东莞|佛山}
{泉州|烟台|唐山|大连}
搜索格式：{城市名} 科技 创新 2026年{MM}月
```

信源优先级：`.gov.cn` 政务官方 > 中科院/工程院/科技日报/中国科技网/行业权威平台 > 新华社/人民网/澎湃/财联社/赛迪/信通院/36氪研究院等。`.gov.cn` 官方原文可作为主来源；媒体智库、转载稿、金额/比例/政策文件名等敏感事实必须尽量找 `.gov.cn` 或权威平台交叉验证，无法验证则删除。

---

## 10. 板块和格式

固定板块名称：

1. 各地科技委动态
2. 上海（长三角）国创中心资讯
3. 科创政策速览
4. 改革举措

要求：日报每板块目标 2-3 条，周报 3-4 条，以可验证条目为准；每板块优先至少 1 条 `.gov.cn`，但不得为达标用旧闻或虚假 URL；每条默认 3 版洞察。

政策速览：必须是正式科技创新政策，标题含《文件名》，`summary` 首句写清发布时间、发布主体、政策名称；不收会议、论坛、活动、调研、泛新闻、无正式文件名报道。

JSON 字段：`title`、`date`、`summary`、`insight`、`source`、`url`。

---

## 11. 洞察写法

每条默认 3 个方案：

- A 行动建议型：牵头部门、对接资源、落地动作。
- B 竞争策略型：对比苏州、无锡、南京、南通等周边城市。
- C 前瞻布局型：面向 3-5 年趋势，指出技术、人才、基础设施布局。

优先结合：AIDC/算力基建、具身智能、未来存储、未来能源、液冷技术、新能源、新能源汽车、高端装备、比亚迪、理想、中创新航、蜂巢能源、三名工程、双高协同、龙城金谷、科教城、高新区、科技金融、成果转化。

禁用空话：值得借鉴、有参考价值、值得关注、值得学习、值得研究、应该加强、应该重视、建议重视、意义重大、影响深远、高度重视、进一步加大、不断深化、持续优化、大力推进。出现则改成具体行动、资源或产业链判断。

---

## 12. 生成后精修

内容删减：用户说“删掉科技委动态整块 / 去掉第一条 / 删除改革举措 / 不用删”等，执行 `修改 JSON → 重新生成 PDF → 重新分发`。

洞察选择：用户回复 `bcca` 等字母串时，从上到下选择对应 A/B/C；删除未选洞察和“方案A/B/C”标签，重新生成 PDF 并分发。

---

## 13. 关键文件

`run_daily.py`、`run_weekly.py`、`run_monthly.py`、`config/settings.yaml`、`config/daily_prompt.md`、`config/weekly_prompt.md`、`config/monthly_prompt.md`、`scripts/generate_html_pdf.py`、`scripts/build_weekly_pdf.py`、`scripts/validate_report.py`、`scripts/fact_check.py`、`scripts/dedup.py`、`scripts/distribute.py`

输出：`~/Desktop/创新情报/日报/`、`~/Desktop/创新情报/周报/`、`~/Desktop/创新情报/月报/`

---

## 14. 最终原则

```text
宁可少写，不可写假。
宁可空板块，不可旧闻充数。
宁可反复核验，不可引用搜索摘要。
宁可继续补搜，不可缩小搜索范围。
宁可询问期号，不可自行编造期号。
```
