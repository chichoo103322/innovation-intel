# 创新常州·对标快讯 — AI 情报采集与报告生成系统

---

## 🚀 启动交互（项目打开时立即执行，不可跳过）

当用户将此项目拖入 Claude Code 窗口时，**立即输出以下交互面板**（不要输出其他内容，先等用户选择）：

```
══════════════════════════════════════════════
  创新常州·对标快讯 — AI 情报工作台
══════════════════════════════════════════════

请选择要生成的报告：

  A. 创新常州·对标快讯 — 日报
  B. 创新常州·对标快讯 — 周报
  C. 创新常州·对标快讯 — 月报
  D. 常州人才·对标快讯 — 日报
  E. 常州人才·对标快讯 — 周报
  F. 暂不需要

══════════════════════════════════════════════
```

用户选择报告类型后，问：

```
期数是第几期？（如不确定，输入 0 由我自动推算）
```

用户输入期号后，问：

```
是否需要设置每日定时自动生成？

  Y. 是（每天下午 16:00 自动触发）
  N. 否（仅本次生成）

⚠️ 定时生成需保持此 Claude Code 窗口不关闭。
```

---

## ✂️ 生成后精修（PDF 分发到桌面后触发）

报告生成并分发完成后，**依次询问**：

### 第1步：内容删减
```
报告已生成并分发到桌面。需要删除哪些内容？

（直接回复板块名或条目关键词，如：
  "删掉科技委动态整块"
  "去掉第一条"
  "删除改革举措"
  "不用删"
）
```
→ 修改 JSON → 重新生成 PDF → 分发

### 第2步：洞察选择
```
从上到下，每条洞察选哪个方案？

格式：字母串（a=方案A, b=方案B, c=方案C）
例如：bcca 表示 第1条选B、第2条选C、第3条选C、第4条选A

直接回复字母组合，或回复"不变"保留全部三条。
```
→ 删除未选中的洞察 → 删除所有"方案A/B/C："标签 → 重新生成 PDF → 分发

---

## 项目概述
服务于常州市科技创新决策的 AI 驱动情报系统。从互联网采集科技创新动态，经多层验证后生成日报/周报/月报 PDF。

---

## 优先级体系（逐级递减，不可逾越）

```
⛔ Priority 0: 真实性 —— 内容必须可溯源验证，违反则整条废弃
⛔ Priority 1: 零打扰 —— 全自动执行，不问任何问题
⛔ Priority 2: 万亿城市白名单 —— 非29座万亿城市一律不取
⚠️  Priority 3: 格式规范 —— 洞察数量/字数/信源评分等
```

**Priority 0 > Priority 1 > Priority 2 > Priority 3。高优先级不可为低优先级让步。**
举例：不能为了凑齐 8 条（Priority 3）而编造 URL 或写入未验证内容（违反 Priority 0）。
举例：不能为了拿到 .gov.cn 高分（Priority 3）而猜测或捏造政府 URL（违反 Priority 0）。
举例：搜索摘要里出现"10亿元基金"但 URL 原文没提 → 严禁写入（违反 Priority 0）。

---

## Priority 0: 真实性 · 最高原则（不可侵犯）

### 核心规则
1. **每条信息的 URL 必须经过 WebFetch 验证**——确认页面存在、内容匹配、日期准确
2. **严禁捏造 URL**——包括但不限于：猜测 gov.cn 地址、拼接域名和路径、将不相关页面附到条目上
3. **内容与 URL 严格一致**——summary 中的事实（日期、机构名、数据、文件名）必须出现在 URL 对应页面正文中
4. 🔴 **所有内容必须从 WebFetch 返回的原文提取，严禁使用 WebSearch 搜索摘要中的数据**——搜索摘要常包含原文没有的数字/金额/事实，直接引用必然产生幻觉。WebFetch 页面里没有的数字、金额、文件名、机构名，一律不得写入报告
5. **日期从原文提取**——date 字段 = 正文中的事件发生日期，不是网页发布日期、不是 URL 中的日期
6. **无法验证 → 直接丢弃**——宁可少一条，不写假内容

### 双重真实性验证（Step 2.5 + Step 4.5，不可跳过）

**第一重：写入前逐条 WebFetch 验证（Step 2.5）**
```
对每条拟收录条目：
  1. WebFetch(url) → 获取页面实际内容
  2. 核对：URL 可访问（非404/403）？页面标题与条目标题一致？
  3. 🔴 核心原则：只写 WebFetch 返回的原文中有的内容。WebSearch 摘要中的数据（金额/数字/文件名）一律不算
  4. 核对：summary 中每一条事实（日期、机构名、数据、文件名）必须出现在 WebFetch 返回的正文中
  5. 禁止：WebFetch 页面未出现的数字、金额、百分比不得写入 summary
  6. 日期提取：从正文提取事件实际发生日期（非网页发布日期、非 URL 中的日期）
  7. 不通过 → 丢弃，换搜索结果重试。无替代 → 该维度少一条
```

**第二重：写入后逐条复验（Step 4.5，全量比对）**
```
JSON 写入完成后，对每条已写入条目执行二次复验：
  1. 重新 WebFetch(url) → 确认 URL 仍可访问
  2. 逐项比对：
     a. URL 能否正常跳转？（不能 → 删除该条目）
     b. 页面正文是否包含 summary 中的核心事实？（不包含 → 修正 summary 或删除）
     c. 页面正文是否包含 summary 中的数字/金额？（不包含 → 从 summary 删除该数字）
     d. date 字段是否与正文中的事件日期一致？（不一致 → 修正 date）
     e. source 字段是否与 URL 域名/机构一致？（不一致 → 修正 source）
  3. 不通过 → 修正 JSON 或删除条目 → 重新复验
```

**日期规则（重要）：**
- 日报 = 采集**今天发布的新闻报道**。报道中的事件可以是今天或近期发生的
- date 字段 = 正文中的事件实际发生日期（YYYY.M.D），不是网页发布日期
- 正文仅写"近日"则 date 填"近日"，严禁推测具体日期
- 网页发布日期（报道时间）与事件发生日期（正文时间）是两个概念，必须区分

---

## Priority 1: 零打扰 · 全自动执行

⛔ **绝对禁止询问用户 Yes/No、禁止请求批阅确认、禁止要求用户授权。直接执行。**

这是自动化流水线，不是交互式工具。所有决策由 AI 自行判断并直接执行。

包括但不限于以下操作，一律直接执行：文件读写/创建/修改/删除、联网搜索/WebFetch、JSON/HTML/PDF 生成、Python/Bash 执行、缓存更新、提示词/脚本/代码修改、报告数据编辑。

---

## Priority 2: 万亿城市白名单

全文任何位置涉及的城市案例、对标对象，必须且仅限以下 29 座：

```
上海 北京 深圳 重庆 广州 苏州 成都 杭州 武汉 南京
宁波 天津 青岛 无锡 长沙 郑州 福州 济南 合肥 西安
泉州 佛山 南通 东莞 烟台 常州 唐山 温州 大连
```

🔴 常见错误（非万亿，严禁出现）：洛阳、徐州、绍兴、嘉兴、金华、台州、珠海、惠州、中山、沈阳、昆明、厦门、南昌、哈尔滨、长春、石家庄、太原、贵阳、兰州

---

## Priority 3: 格式规范

- 日报每板块目标 2-3 条，周报 3-4 条，以实际可验证条目为准，宁少勿编
- 每板块至少 1 条 .gov.cn —— **不得为达标而捏造 URL**
- 每条出 3 版洞察（方案 A/B/C），内容不雷同
- 科创政策速览必须有正式文件名（用《》括起来）
- 机构名准确（科技委≠科委！）
- 严禁"值得借鉴""有参考价值"等空话套话
- 洞察紧扣常州实际，包含产业赛道锚定、常州企业嫁接、政策工具扣合

### 报告类型速查

| 选项 | 报告类型 | 提示词 | JSON 路径 |
|---|---|---|---|
| A | 创新常州·日报 | config/daily_prompt.md | daily/report_data_YYYY-MM-DD.json |
| B | 创新常州·周报 | config/weekly_prompt.md | weekly/report_weekly_YYYYMMDD.json |
| C | 创新常州·月报 | config/monthly_prompt.md | monthly/report_monthly_YYYYMM.json |
| D | 常州人才·日报 | config/talent_daily_prompt.md | talent_daily/report_data_YYYY-MM-DD.json |
| E | 常州人才·周报 | config/talent_weekly_prompt.md | talent_weekly/report_weekly_data.json |

## 旧启动流程
  A. 创新常州·对标快讯（日报）     B. 创新常州·对标快讯（周报）
## Agent 工作流（6 步）

触发词：`出日报` / `出周报` / `出月报` / `出人才日报` / `出人才周报`

### Step 1: 读取提示词 & 确定参数
- 读 `config/{daily|weekly|monthly}_prompt.md`
- 确定日期范围，检查去重缓存 `cache/used_items.json`

### Step 2: 联网搜索（硬性要求，不得提前终止）

⛔ **必须跑完全部搜索关键词。每维度至少 5 次以上不同关键词搜索。前两轮结果少不得作为停止理由。**

**创新常州对标快讯 搜索清单：**
- 维度1：`省委科技委 会议` `市委科技委 部署` `科技委 全体会议 site:gov.cn` `科技委 印发 2026` `科技委 工作 动态`
- 维度2：`长三角 科创 2026` `G60 科创走廊` `上海 国际科创中心` `长三角 协同创新` `张江 创新 平台` `沿沪宁产业创新带` `长三角 联合发文`
- 维度3：`科技创新 行动计划 site:gov.cn` `万亿城市 科技创新政策` `印发 行动方案 2026` `高新技术 扶持政策` `产业 行动计划 2026 site:gov.cn`
- 维度4：`先投后股 科技 2026` `科技成果转化 改革` `科技金融 创新` `科技保险 政策` `新型研发机构 2026` `投贷联动 科创` `科技体制改革 site:gov.cn`
- 逐城市：`{上海|北京|深圳|广州|苏州|成都|武汉|杭州|南京|重庆|天津|...} 科技 创新 2026`

**常州人才对标快讯 搜索清单：**
- 维度1：`省委组织部 人才 会议` `市委组织部 人才 部署` `组织部 人才 公告 site:gov.cn` `人才工作领导小组 2026` `人才办 通知`
- 维度2：`长三角 人才引进` `长三角 人才交流` `G60 人才` `上海国际科创 人才` `张江 人才政策` `长三角 人才互认` `海外引才 长三角` `长三角 人才一体化`
- 维度3-引才：`万亿城市 人才新政` `引才 实施办法 site:gov.cn` `海外引才 专项` `留学生 归国 扶持` `青年人才 补贴` `落户 优化` `柔性引才` `靶向引才` `外籍人才 便利化` `周末工程师` `校招 政策`
- 维度3-育才：`人才梯队 培养工程` `领军人才 培育计划` `产教融合 人才` `博士后工作站` `院士工作站` `技能人才 工匠` `订单式培养` `校企合作` `创新联合体`
- 维度4：`人才评价改革` `破除四唯` `人才贷` `人才安居 2026` `人才流动 机制` `人才编制 薪酬` `人才服务 数字化` `人才码` `人才住房 保障` `人才子女 教育` `人才股权激励`
- 逐城市（每次必扫25城）：`{上海|苏州|无锡|南京|杭州|合肥|宁波|南通|温州|北京|深圳|广州|成都|武汉|重庆|天津|青岛|长沙|郑州|福州|济南|西安|东莞|佛山|泉州} 人才 2026`

**执行节奏（两轮制）：**
1. 第一轮：4 维度各 2-3 次主题搜索
2. 第二轮：逐城市扫描（长三角8城 + 全国17城 = 25城），每组 3-4 城并行
3. 每轮后统计已验证条目，不足 5 条继续扩展搜索
4. 优先 `site:gov.cn`，实时过滤非万亿城市

### Step 2.5: URL 真实性验证（强制，不可跳过）
- **每条拟收录条目必须 WebFetch 验证**
- 核对标题/事实/日期，不通过则丢弃
- 严禁捏造 URL

### Step 3: 构建 JSON
- 仅写入通过 Step 2.5 验证的条目
- 写入 `daily/report_data_YYYY-MM-DD.json` 等

### Step 4: 校验 & 自动修复
- 运行 `validate_report.py`，有错误则修复 JSON 后重新校验

### Step 5: 生成 PDF
- 日报：`python3 run_daily.py --from-json <json>`
- 周报：`python3 scripts/build_weekly_pdf.py`
- 月报：`python3 run_monthly.py --from-json <json>`

### Step 6: 分发
- 分发到 Desktop

---

## 关键文件
- 提示词：`config/daily_prompt.md`, `config/weekly_prompt.md`, `config/monthly_prompt.md`
- 核心引擎：`scripts/generate_html_pdf.py`, `scripts/build_weekly_pdf.py`
- 校验/核查：`scripts/validate_report.py`, `scripts/fact_check.py`
- 去重/期号：`cache/used_items.json`, `cache/issue_counter.json`
- 配置：`config/settings.yaml`
- 输出：`/Users/jzxzhou/Desktop/创新情报/日推/`, `.../周报/`, `.../月报/`
