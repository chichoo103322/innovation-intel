# 《创新常州·对标快讯》部署手册

AI 驱动的科技创新情报系统。每日自动联网搜索 → 分析 → 生成 PDF 报告 → 分发到桌面。

## 系统要求

- Python 3.10+
- Google Chrome（用于 HTML → PDF 渲染）
- macOS / Windows / Linux

## 一、安装 Python 依赖

```bash
pip3 install openai python-docx
```

> 注：`openai` 用于调用 DeepSeek API（OpenAI 兼容接口），`python-docx` 仅用于期号计算等辅助功能。

## 二、获取 DeepSeek API Key

1. 访问 https://platform.deepseek.com 注册账号
2. 进入「API Keys」→ 创建新 Key
3. 复制 `sk-` 开头的密钥

**方式 A（推荐）**：设置环境变量
```bash
export DEEPSEEK_API_KEY="sk-你的密钥"
```

**方式 B**：写入配置文件
编辑 `config/settings.yaml`：
```yaml
deepseek_api_key: "sk-你的密钥"
```

## 三、配置项目

编辑 `config/settings.yaml`：

```yaml
# 期号起始日期（系统按工作日自动计算）
first_issue_date: "2026-07-05"

# 桌面输出路径（报告自动按日报/周报/月报分子目录）
desktop_output_dir: "/Users/你的用户名/Desktop/创新情报"
```

## 四、确认 Chrome 路径

脚本默认使用 `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`。
Windows/Linux 用户需修改 `scripts/generate_html_pdf.py` 中 `html_to_pdf()` 函数里的 Chrome 路径。

## 五、安装定时任务

### Mac
```bash
bash scripts/setup_scheduler.sh
```

### Windows
```powershell
powershell -ExecutionPolicy Bypass -File scripts/setup_scheduler.ps1
```

定时规律：

| 报告 | 时间 | 机制 |
|------|------|------|
| 日报 | 每 30 分钟检测（工作日，已生成则跳过） | launchd / Task Scheduler |
| 周报 | 周五 17:00 | cron |
| 月报 | 月末最后一天 17:00 | cron |

## 六、手动运行

```bash
# 日报（联网搜索 → HTML → PDF → 桌面）
python3 run_daily.py

# 周报
python3 scripts/generate_html_pdf.py

# 月报
python3 scripts/generate_html_pdf.py --monthly

# 使用示例数据（不消耗 API）
python3 run_daily.py --dry-run
python3 scripts/generate_html_pdf.py --sample
```

## 七、项目结构

```
innovation-intel/
├── run_daily.py                  # 日报主入口
├── run_weekly.py                 # 周报主入口（汇总日报）
├── run_monthly.py                # 月报主入口
├── config/settings.yaml          # 配置文件
├── scripts/
│   ├── generate_html_pdf.py      # HTML→PDF 生成（日报/周报/月报共用）
│   ├── generate_docx.py          # Word 生成（辅助）
│   ├── generate_pdf.py           # fpdf2 备用方案
│   ├── dedup.py                  # 去重模块
│   ├── distribute.py             # 桌面分发模块
│   ├── setup_scheduler.sh        # Mac 定时任务安装
│   └── setup_scheduler.ps1       # Windows 定时任务安装
├── daily/                        # 日报输出
├── weekly/                       # 周报输出
├── monthly/                      # 月报输出
└── cache/                        # 缓存 & 日志
```

## 八、日常维护

| 事项 | 操作 |
|------|------|
| 充 API 余额 | https://platform.deepseek.com |
| 改搜索偏好 | 编辑 `config/settings.yaml` |
| 手动补跑日报 | `python3 run_daily.py --force` |
| 暂停定时任务（Mac） | `bash scripts/setup_scheduler.sh --uninstall` |
| 查看日志 | `cat cache/launchd.log` |

> 电脑需要保持开机。睡眠期间任务会跳过，唤醒后 launchd 在 30 分钟内自动补跑。
