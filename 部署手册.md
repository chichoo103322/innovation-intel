# 《创新常州·对标快讯》部署手册

科技创新情报系统。采集内容 → JSON → HTML → PDF → 分发到桌面。

## 系统要求

- Python 3.10+
- Google Chrome 或 Chromium（任一即可，用于 HTML → PDF 渲染）
- macOS / Windows / Linux

## 一、安装 Python 依赖

```bash
python3 -m pip install -r requirements.txt
```

> 渲染器会自动探测 Chrome 与 Chromium，不再依赖 macOS 专用路径。若浏览器装在自定义位置，可设置环境变量 `REPORT_BROWSER_PATH` 指向浏览器可执行文件。

## 二、配置项目

编辑 `config/settings.yaml`：

```yaml
# 期号起始日期
first_issue_date: "2026-07-05"

# 留空则自动分发到当前系统桌面；也可填绝对路径
desktop_output_dir: ""
```

## 三、确认浏览器与输出目录

脚本会自动探测 macOS、Windows 与 Linux 上的 Chrome 或 Chromium；Windows 不需要改代码。可选环境变量：`REPORT_BROWSER_PATH` 指定浏览器路径，`REPORT_DESKTOP_DIR` 指定报告分发根目录（适合 OneDrive 重定向桌面或服务器）。

## 四、安装定时任务

### Mac
```bash
bash scripts/setup_scheduler.sh
```

### Windows
```powershell
powershell -ExecutionPolicy Bypass -File scripts/setup_scheduler.ps1
```

定时规律：

| 报告 | 时间 | 机制 |
|------|------|------|
| 日报 | 每 30 分钟检测（工作日，已生成则跳过） | launchd / Task Scheduler |
| 周报 | 周五 17:00 | cron |
| 月报 | 月末最后一天 17:00 | cron |

## 五、手动运行

```bash
# 日报（从 JSON 生成 HTML → PDF）
python3 run_daily.py --from-json daily/report_data_20260706.json

# 输出 JSON 模板（填写后用于生成）
python3 run_daily.py --collect

# 示例数据快速预览
python3 run_daily.py --sample

# 周报
python3 run_weekly.py --json weekly/report_weekly_20260706.json

# 月报
python3 run_monthly.py --json monthly/report_monthly_20260706.json
```

> Windows 可将上述 `python3` 替换为 `py` 或 `python`。

## 六、项目结构

```
innovation-intel/
├── run_daily.py                  # 日报主入口（JSON → HTML → PDF）
├── run_weekly.py                 # 周报主入口（JSON → HTML → PDF）
├── run_monthly.py                # 月报主入口（JSON → HTML → PDF）
├── config/
│   ├── settings.yaml             # 项目配置
│   └── daily_prompt.md           # 情报采集提示词规范
├── scripts/
│   ├── generate_html_pdf.py      # HTML→PDF 生成（日报/周报/月报共用）
│   ├── build_weekly_pdf.py       # 周报 HTML 构建
│   ├── validate_report.py        # 后处理校验
│   ├── fact_check.py             # 事实核查
│   ├── crawler.py                # 信源爬虫
│   ├── dedup.py                  # 去重模块
│   ├── distribute.py             # 桌面分发模块
│   ├── setup_scheduler.sh        # Mac 定时任务安装
│   └── setup_scheduler.ps1       # Windows 定时任务安装
├── daily/                        # 日报输出
├── weekly/                       # 周报输出
├── monthly/                      # 月报输出
└── cache/                        # 缓存 & 日志
```

## 七、工作流程

1. 人工采集/整理情报数据，按 JSON 模板填写各维度条目
2. 运行 `--from-json` 命令，系统自动完成 HTML 渲染 → PDF 导出
3. PDF 自动分发到桌面（日报/周报/月报子目录）

## 八、日常维护

| 事项 | 操作 |
|------|------|
| 手动补跑日报 | `python3 run_daily.py --from-json daily/report_data_xxx.json --force` |
| 暂停定时任务（Mac） | `bash scripts/setup_scheduler.sh --uninstall` |
| 查看日志 | `cat cache/launchd.log` |

> 电脑需要保持开机。睡眠期间任务会跳过，唤醒后 launchd 在 30 分钟内自动补跑。
