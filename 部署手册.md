# 《创新常州·对标快讯》部署手册

## 一、安装 Python（5分钟）

### Windows
1. 打开 https://www.python.org/downloads/
2. 点击黄色按钮下载，双击安装
3. **⚠️ 务必勾选底部的「Add Python to PATH」**，然后点 Install Now
4. 完成后重启电脑

### Mac
1. 打开终端（在"启动台 → 其他"里找 Terminal）
2. 粘贴执行：`brew install python3`
3. 如果没有 Homebrew，先去 https://brew.sh 按提示安装

---

## 二、配置项目（10分钟）

### 1. 获取 DeepSeek API Key
- 访问 https://platform.deepseek.com 注册账号
- 进入「API Keys」→「创建 API Key」→ 复制 `sk-...` 开头的密钥

### 2. 编辑配置文件
打开项目文件夹里的 `config/settings.yaml`，修改以下两项：

```yaml
deepseek_api_key: "sk-你的密钥粘贴在这里"

first_issue_date: "2026-07-06"   # 第一期日期，按工作日自动编号
```

### 3. 修改桌面输出路径

同一文件里找到这一行，改成你电脑桌面的路径：

```yaml
# Windows
desktop_output_dir: "C:/Users/你的用户名/Desktop/创新情报"

# Mac
desktop_output_dir: "/Users/你的用户名/Desktop/创新情报"
```

报告会自动存到子目录：
```
桌面/创新情报/
├── 日报/
├── 周报/
└── 月报/
```

### 4. 安装依赖

Windows：打开 CMD（Win+R 输入 cmd），进入项目目录：
```
pip3 install python-docx openai httpx
```

Mac：终端进入项目目录：
```
pip3 install python-docx openai httpx
```

---

## 三、安装定时任务（1分钟）

### Windows
1. 在开始菜单搜索 `PowerShell`，右键 → **以管理员身份运行**
2. 进入项目目录（例如 `cd C:\Users\你的用户名\Desktop\innovation-intel`）
3. 执行：
```
powershell -ExecutionPolicy Bypass -File scripts/setup_scheduler.ps1
```

### Mac
终端进入项目目录：
```
bash scripts/setup_scheduler.sh
```

安装后定时规律：

| 报告 | 时间 |
|------|------|
| 日报 | 工作日 8:30 |
| 周报 | 周五 17:00 |
| 月报 | 月末 17:00 |

---

## 四、测试

### Windows
CMD 进入项目目录：
```
python3 run_daily.py
```

### Mac
终端进入项目目录：
```
python3 run_daily.py
```

执行后会看到搜索过程，等待 1-2 分钟后完成，Word 文档自动保存到桌面。

---

## 五、日常维护

| 事项 | 操作 |
|------|------|
| 充 API 余额 | platform.deepseek.com |
| 改搜索偏好 | 编辑 `config/settings.yaml` |
| 手动重新生成当天日报 | 进入项目目录执行 `python3 run_daily.py` |
| 卸载定时任务（Win） | PowerShell 管理员执行 `powershell -ExecutionPolicy Bypass -File scripts/setup_scheduler.ps1 -Action Uninstall` |
| 卸载定时任务（Mac） | `bash scripts/setup_scheduler.sh --uninstall` |

> **注意**：定时任务执行时电脑需要保持开机状态。如果电脑关机或休眠，当次任务会跳过，下次恢复正常。
